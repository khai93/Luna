import { IExecuteable } from "../../../common/interfaces/IExecuteable";

export class ExpressRegistryComponent implements IExecuteable {
    execute(): void {
        throw new Error("Method not implemented.");
    }
}